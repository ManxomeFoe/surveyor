<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.surveyor.app"
    android:versionCode="9"
    android:versionName="1.8">

    <!-- Survey data stays on-device. INTERNET is held ONLY for the v1.3
         self-update download from GitHub releases; REQUEST_INSTALL_PACKAGES
         lets the app hand the downloaded APK to the system installer. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- v1.6 blue-dot live location (foreground only, no background location). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="false"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|density|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Serves ONLY filesDir/update.apk to the system package installer
             (access granted per-intent via FLAG_GRANT_READ_URI_PERMISSION). -->
        <provider
            android:name=".ApkProvider"
            android:authorities="com.surveyor.app.updates"
            android:exported="false"
            android:grantUriPermissions="true" />

    </application>
</manifest>
